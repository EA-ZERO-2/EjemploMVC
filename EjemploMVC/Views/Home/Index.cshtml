@using AdventureWorksNS.Data;
@model HomeIndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<p class="alert alert-primary">@DateTime.Now.ToLongTimeString()</p>
@*
<div id="CarouselExampleControls" class="carousel" data-ride="carousel">
<ol class="carousel-indicators"></ol>


    <div class="carousel-inner">
        @if (Model.Categorias is not null)
        {
            @foreach(ProductCategory productCategory in Model.Categorias)
            {
                <div class="carousel-item">
                    <img src="~/img/62035.jpg" class="d-block w-100" alt="img 1">
                </div>
            }

        }
    </div>
</div>*@

<div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="~/img/62035.jpg" class="d-block w-100" alt="img 1">
            <h5>Titulo uno</h5>
        </div>
        <div class="carousel-item">
            <img src="~/img/62154.jpg" class="d-block w-100" alt="img 2">
            <h5>Titulo dos</h5>
        </div>
        <div class="carousel-item">
            <img src="~/img/maxresdefault.jpg" class="d-block w-100" alt="img 3">
            <h5>Titulo tres</h5>
        </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>


@if (Model.Categorias is not null)
{
    <h2>Categorias</h2>
    @foreach(ProductCategory prdCat in Model.Categorias)
    {
        <p>@prdCat.Name</p>
    }
}
<h2>Visitas:@Model.ContadorVisitas</h2>


